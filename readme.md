# JavaScript Polyfills

This repository contains a collection of JavaScript polyfills. Custom implementations of built-in JavaScript methods from scratch.

## Categories

- **Array** — core array methods
- **Function** — utility function patterns
- **Promise** — async primitives
- **Object** — object utility methods
- **String** — string prototype methods
